{
  "name": "backend",
  "version": "1.0.0",
  "description": "",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node server.js",
    "server": "nodemon server.js",
    "client": "npm start --prefix ../spotify-player",
    "auth": "node app.js --prefix ../web-api-auth-examples/authorization_code",
    "dev": "concurrently \"npm run server\" \"npm run client\" \"npm run auth\"",
    "heroku-postbuild": "NPM_CONFIG_PRODUCTION=false npm install -- prefix spotify-player && npm run build -- prefix spotify-player"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "concurrently": "^6.0.0",
    "cors": "^2.8.5",
    "dotenv": "^8.2.0",
    "express": "^4.17.1",
    "mongoose": "^5.11.8"
  }
}
